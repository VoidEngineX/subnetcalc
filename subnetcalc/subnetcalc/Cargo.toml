[package]
name = "subnetcalc"
version = "0.1.0"
edition = "2024"
description = "Fast IPv4 subnet calculator in Rust"
license = "MIT"

[dependencies]
clap = {version = "4.5.54", features = ["derive"]}
